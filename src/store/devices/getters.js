export function devices(state) {
  return state.devices;
}
export function deviceCategories(state) {
  return state.device_categories;
}

export function deviceManufacturers(state) {
  return state.device_manufacturers;
}
